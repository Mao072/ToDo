	// DataSeeder removed. Seeder logic has been deleted as requested.
	// If you want to re-add seeding, create a new file with the seeding logic
	// and call it explicitly from a development-only tool or script.

	public static class DataSeeder
	{
		public static Task SeedAsync()
		{
			// No-op placeholder to avoid compile errors where the type might be referenced.
			return Task.CompletedTask;
		}
	}
